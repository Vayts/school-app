export const getUserToken = (state) => state.auth.user.token;
export const getUserName = (state) => state.auth.user.firstName;
export const getUserRole = (state) => state.auth.user.role;
export const getUserInfo = (state) => state.auth.user;
